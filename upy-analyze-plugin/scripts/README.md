# `upy-analyze-plugin/scripts`

本目录预留给插件化工作流版 analyze 使用的脚本。

第一批重点脚本：

1. `init_manifest.py`
   - 从“写盘脚本”改为“校验器/规范化器”
   - 目标支持 stdin 输入
   - 输出结构化 JSON 结果

当前先保留目录骨架，不覆盖原 `G:\MicroPython_Skills\upy-analyze\scripts`。
