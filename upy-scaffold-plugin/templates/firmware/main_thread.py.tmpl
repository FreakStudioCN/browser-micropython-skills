# Python env   : ${MICROPYTHON_VERSION_LABEL}
# -*- coding: utf-8 -*-
# @Generated : upy-scaffold ${GENERATED_AT} (mode: thread)
# @File    : main.py
# @Description : Hardware assembly and _thread scheduler frame
# @License : MIT

import _thread
import sys
import time
from machine import I2C, Pin

import conf
${LOGGER_IMPORTS}
${MAINTENANCE_IMPORT}


${LOGGER_HELPERS_BLOCK}


def _main():
    ${LOGGER_SETUP_BLOCK}
    ${THREAD_LOG_BOOT_BLOCK}

    # Hardware instances generated from manifest.pinout.
${I2C_INIT_BLOCK_IN_MAIN}
${GPIO_INIT_BLOCK_IN_MAIN}

    # TODO: upy-generate starts business threads here with _thread.start_new_thread().
    while True:
${MAINTENANCE_THREAD_BLOCK}
        time.sleep_ms(100)


try:
    _main()
except Exception as exc:
    _log_exception(exc, "[fatal] main.py startup failed")
    raise
