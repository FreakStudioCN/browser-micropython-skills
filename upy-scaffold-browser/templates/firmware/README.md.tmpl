# ${PROJECT_NAME}

> Generated by upy-scaffold | mode: ${MODE} | ${GENERATED_AT}

## Hardware

- MCU: ${MCU_MODEL} (${MCU_BOARD})
- Firmware: ${FIRMWARE_URL}

## BOM

| # | Name | Model | Qty | Price (yuan) |
|---|------|-------|-----|-------------|
${BOM_TABLE_ROWS}

**Total: ${TOTAL_PRICE} yuan**

## Pinout

| Device | Pin | GPIO | Bus | I2C Addr |
|--------|-----|------|-----|----------|
${PINOUT_TABLE_ROWS}

## Scheduler Mode

`${MODE}`

## Quick Start

Deploy this project from Blockless Web Builder: the `upy-deploy-browser` skill
uploads `firmware/` to the device and starts it over the device session — no host
serial tooling or local CLI required. Device logs (`/log/run_*.log`) are read back
through the same device session.
