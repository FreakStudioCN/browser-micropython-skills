# Python env   : ${MICROPYTHON_VERSION_LABEL}
# -*- coding: utf-8 -*-
# @Generated : upy-scaffold ${GENERATED_AT} (mode: timer)
# @File    : main.py
# @Description : Hardware assembly and Timer scheduler frame
# @License : MIT

import sys
import time
from machine import I2C, Pin

import conf
${LOGGER_IMPORTS}
from lib.scheduler.timer_sched import Scheduler
${MAINTENANCE_IMPORT}


${LOGGER_HELPERS_BLOCK}


def _main():
    ${LOGGER_SETUP_BLOCK}
    ${LOG_BOOT_BLOCK}

    # Hardware instances generated from manifest.pinout.
${I2C_INIT_BLOCK_IN_MAIN}
${GPIO_INIT_BLOCK_IN_MAIN}

    sc = Scheduler(${SCHEDULER_INIT_ARGS})
    # TODO: upy-generate registers business tasks here.
    sc.start()


try:
    _main()
except Exception as exc:
    _log_exception(exc, "[fatal] main.py startup failed")
    raise
